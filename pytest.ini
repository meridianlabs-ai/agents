[pytest]
# The only Python under test is .github/scripts/validate_manifest.py (the
# land job's manifest validator). Run with `python3 -m pytest` from the repo
# root; CI runs the same command (see tests/README.md).
testpaths = tests
addopts = -q
